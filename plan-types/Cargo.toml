[package]
name = "protogen-plan"
version = "0.1.0"
edition = "2021"

[lib]
name = "protogen_plan"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
